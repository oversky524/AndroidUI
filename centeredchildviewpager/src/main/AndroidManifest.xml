<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.oversky524.centeredchildviewpager">

    <application
        android:allowBackup="false"
        android:supportsRtl="true">
        <activity android:name=".MyViewPagerActivity"
            android:label="@string/my_view_pager_demo">
            <intent-filter>
                <action android:name="android.intent.action.UiDemos" />
            </intent-filter>
        </activity>
    </application>

</manifest>
